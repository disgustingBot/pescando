<?php
  // // Script que marca la pantalla como activa en la bbdd
  // if ( $conn && $ipPantalla != '' ) {
  //   @mysqli_query($conn, "UPDATE pesca_screens SET scr_last_active = '".date("YmdHis")."' WHERE scr_ip = '".$ipPantalla."'");
  // }
 ?>
